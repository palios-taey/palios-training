# RETIRED (PR32 rework) — do not use for gating decisions.
#
# The r5-audit-gate workflow no longer reads this file. Every nonempty PR diff
# is R5-risky by default. The previous allowlist missed 34 of 269 tracked files
# (including moe-35b production trainers/recipes/configs) because shell
# `case "$f" in $pat)` is path-prefix anchored (`trainers/*` never matched
# `dense-9b/trainers/...`).
#
# Kept as a tombstone so searches for r5-risky-paths still surface the defect
# and the replacement rule. Safe to delete in a later cleanup once docs no
# longer reference the path.
