# Corpora quarantined BY CONTENT. One sha256 per line, optional "# note".
#
# WHY DIGESTS AND NOT PATHS: the same bytes existed at two paths on 2026-08-02, one under a
# directory marker and one not. A path-keyed control protects a location; a digest follows the
# bytes through any copy or rename.
#
# This file is the OPERATIVE INPUT to the trainer's content gate. It was missing for the first
# hours of that gate's life, which made the gate inert while looking present — the commit was
# real, the code was real, the docstring even named the sha, and the list it read was empty.
# The trainer now REFUSES TO START if this file is absent or unreadable, so that cannot recur.
#
# Removing a line here un-quarantines a corpus. That is a corpus-owner decision (treasurer),
# not a convenience for unblocking a run.

cdb345826b6d6b11d7a4e25f26c0342fab720877f555b4541fbbb1740d2357b6  # stage2_all_sanctioned_v1.jsonl, 9887 rows — treasurer fl-cred-corpus-quarantine (OPEN): plaintext host credentials in the ASSISTANT role, i.e. as training targets
