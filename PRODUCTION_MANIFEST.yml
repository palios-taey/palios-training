# PRODUCTION_MANIFEST.yml — public executable capability map
#
# Lists runnable public entry points only. Private adjudication receipts, dose ledgers,
# and corpus digests live under GOVERNED_SFT_ROOT and are not mirrored here.
# Field names match scripts/_resolve_capability.py (status + entrypoint).
schema: 1
status: public-executable-map
capabilities:
  cpt_27b_4node:
    status: ADJUDICATED
    entrypoint: dense-9b/recipes/launch_cpt_qwen36_27b_fsdp.sh
    description: 4-node FSDP CPT launcher (public recipe)
  bake_27b:
    status: ADJUDICATED
    entrypoint: dense-9b/recipes/bake_27b.sh
    description: bake/export launcher (public recipe)
